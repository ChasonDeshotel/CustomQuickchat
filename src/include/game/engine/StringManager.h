FString
NewFString(
    const std::string& str); // have the game create an FString (using UE stuff), or else it'll crash when used with objects/params
FString
NewFString(const FString& old); // have the game create an FString (using UE stuff), or else it'll crash when used with objects/params
FName
FindFName(const std::string& str);
